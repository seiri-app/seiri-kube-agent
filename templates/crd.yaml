---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: healthchecks.monitoring.seiri.app
spec:
  group: monitoring.seiri.app
  names:
    kind: HealthCheck
    listKind: HealthCheckList
    plural: healthchecks
    singular: healthcheck
    description: Kubernetes custom resource definition representing a HealthCheck object used by Seiri monitoring system
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
      description: Current computed health phase of the target resource
    - jsonPath: .status.lastCheckTime
      name: Last Check
      type: date
      description: Timestamp of the last executed health check
    - jsonPath: .status.lastSuccessTime
      name: Last Success
      type: date
      description: Timestamp of the last successful health check
    - jsonPath: .status.lastFailedTime
      name: Last Failed
      type: date
      description: Timestamp of the last failed health check attempt
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
      description: Time elapsed since the HealthCheck resource was created
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HealthCheck is the Schema for the healthchecks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HealthCheck
            properties:
              checkID:
                type: string
              interval:
                type: string
              suspend:
                type: boolean
              tags:
                additionalProperties:
                  type: string
                type: object
              targetRef:
                description: TargetRef refers to the Kubernetes resource to check
                  (Deployment, Pod, Job)
                properties:
                  apiVersion:
                    description: APIVersion is the apiVersion of the target resource
                      (e.g., apps/v1)
                    type: string
                  kind:
                    description: Kind is the kind of the target resource (e.g., Deployment)
                    type: string
                  name:
                    description: Name is the name of the target resource
                    type: string
                  namespace:
                    description: 'Namespace is the namespace of the target resource
                      (default: same as HealthCheck)'
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - checkID
            - targetRef
            type: object
          status:
            description: status defines the observed state of HealthCheck
            properties:
              clusterID:
                description: ClusterID is the identifier of the cluster where the
                  check is running
                type: string
              conditions:
                description: conditions represent the current state of the HealthCheck
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastCheckTime:
                description: LastCheckTime is the timestamp of the last health check
                format: date-time
                type: string
              lastFailedTime:
                description: LastFailedTime is the timestamp of the last failed health
                  check
                format: date-time
                type: string
              lastSuccessTime:
                description: LastSuccessTime is the timestamp of the last successful
                  health check
                format: date-time
                type: string
              message:
                description: Message is a human-readable description of the current
                  status
                type: string
              observedGeneration:
                description: ObservedGeneration is the last generation of the resource
                  that was reconciled.
                format: int64
                type: integer
              phase:
                description: Phase is the current status of the target resource.
                enum:
                - Healthy
                - Unhealthy
                - Missing
                - Error
                - Unknown
                - Suspended
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
